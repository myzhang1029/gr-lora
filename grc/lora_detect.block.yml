# auto-generated by grc.converter

id: lora_detect
label: LoRa Detector
category: '[lora]'

parameters:
-   id: spreading_factor
    label: Spreading Factor
    dtype: int
    default: '8'
-   id: beta
    label: FFT Window Beta
    dtype: float
    default: '25.0'
-   id: fft_factor
    label: FFT Size Factor
    dtype: int
    default: '10'
-   id: peak_search_algorithm
    label: Peak Search Algorithm
    dtype: enum
    options: ['0', '1', '2']
    option_labels: [ABS, PHASE, B]
-   id: peak_search_phase_k
    label: Peak Search PHASE K
    dtype: int
    default: '4'
-   id: fs_bw_ratio
    label: Samp-BW ratio
    dtype: float
    default: '2'
-   id: on_length
    label: Length in (unit) to stay open after a preamble is detected
    dtype: int
    default: '1'
-   id: filename_template
    label: Filename Template (strftime format, XXXX will become a unique string)
    dtype: string
    default: '%Y-%m-%d_%H%M%S_XXXX.dat'

inputs:
-   domain: stream
    dtype: complex

templates:
    imports: from gnuradio import lora
    make: lora.detect(${spreading_factor}, ${beta}, ${fft_factor}, ${peak_search_algorithm},
        ${peak_search_phase_k}, ${fs_bw_ratio}, ${on_length}, ${filename_template})

file_format: 1
